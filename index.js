let mazeA = [
    ["#", "#", "#", "#", "#"],
    ["%", "0", "0", "0", "@"],
    ["#", "#", "#", "0", "#"],
    ["#", "#", "#", "0", "#"],
    ["#", "#", "#", "#", "#"],
];
let mazeB = [
    ["#", "0", "#", "#", "#"],
    ["%", "0", "#", "0", "@"],
    ["#", "0", "#", "0", "#"],
    ["#", "0", "0", "0", "#"],
    ["#", "#", "#", "#", "#"],
];

function laberinto(lab) {
    if (lab[x][y] === "@") {
        return true;
    }
    if (lab[x][y] === "#") {
        return false;
    }
    if (x > 4 || x < 0||y > 4||y < 0) {
        return false;
    }
    lab[x][y] = "+";
    if(laberinto())

}
